{
  "name": "first_server",
  "version": "1.0.0",
  "description": "mishka's first server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mishka morgan",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
